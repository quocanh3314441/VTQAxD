-- Winter map
local currentversion = "Garden Tower Defense-VTQAxD"


-- Add vector.create compatibility if not present
if not vector or not vector.create then
    vector = {}
    function vector.create(x, y, z)
        return Vector3.new(x, y, z)
    end
end

-- Disable 3D rendering
    game:GetService("RunService"):Set3dRenderingEnabled(false)

-- Services
local Players = game:GetService("Players")

-- Anti-AFK setup
local virtualUser = game:GetService("VirtualUser")
local antiAfkEnabled = true
local timeInAfk = 0

local function startAfkTimer()
    spawn(function()
        while antiAfkEnabled do
            wait(1)
            timeInAfk = timeInAfk + 1
        end
    end)
end

-- Auto low graphics
-- (moved) Auto low graphics: will be applied once on join below after local flags

local function startAntiAfk()
    spawn(function()
        while antiAfkEnabled do
            wait(60)
            virtualUser:CaptureController()
            virtualUser:ClickButton2(Vector2.new())
        end
    end)
end

-- Start anti-AFK
startAntiAfk()
startAfkTimer()

-- Local var
local autojoinmap = true
local autojoindojo = true
local autodifficulty = true
local autospeedchange = true
local autolowgraphics = true
local autoskipwaves = true
local autorestart = true
local autowindojo = true

-- Auto low graphics (apply once on join)
if autolowgraphics then
    task.spawn(function()
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local function applyOnce()
            pcall(function()
                ReplicatedStorage:WaitForChild("RemoteFunctions"):WaitForChild("ChangeSetting"):InvokeServer("st_graphics", 2)
            end)
        end

        local player = Players.LocalPlayer
        if player then
            if player.Character then
                applyOnce()
            else
                player.CharacterAdded:Wait()
                applyOnce()
            end
        else
            Players.PlayerAdded:Wait()
            player = Players.LocalPlayer
            if player then
                player.CharacterAdded:Wait()
                applyOnce()
            end
        end
    end)
end

-- Rafflesia tracking state
local flowerConfigs = {
    {
        id = 1,
        position = vector.create(-38.81913757324219, -85.18521881103516, 28.513927459716797),
        cf = CFrame.new(-38.81913757324219, -85.18521881103516, 28.513927459716797, -1, 0, -8.742277657347586e-08, 0, 1, 0, 8.742277657347586e-08, 0, -1),
        rotation = 180
    },
    {
        id = 2,
        position = vector.create(-33.78114318847656, -85.18521881103516, 39.39654541015625),
        cf = CFrame.new(-33.78114318847656, -85.18521881103516, 39.39654541015625, -1, 0, -8.742277657347586e-08, 0, 1, 0, 8.742277657347586e-08, 0, -1),
        rotation = 180
    }
}

-- Get the number of placed Flowers using the Units label's Text property
local function getPlacedFlowerCount()
    local player = Players.LocalPlayer
    if not player then return 0 end
    local units = player:FindFirstChild("PlayerGui")
    if units then
        units = units:FindFirstChild("GameGui")
    end
    if units then
        units = units:FindFirstChild("Screen")
    end
    if units then
        units = units:FindFirstChild("Bottom")
    end
    if units then
        units = units:FindFirstChild("Units")
    end

    if units and (units:IsA("TextLabel") or units:IsA("TextButton")) then
        local text = units.Text
        local placed = text:match("^(%d+)%s*/%s*%d+")
        return tonumber(placed) or 0
    end
    return 0
end

-- Randomize position by ±5% of X and Z (not based on distanceAlongPath)
local function randomizePosition(config)
    local x = config.position.X
    local y = config.position.Y
    local z = config.position.Z
    local xOffset = (math.random() - 0.5) * 0.10 * math.abs(x)
    local newX = x + xOffset
    local newPos = vector.create(newX, y, z)
    local _, _, _, r00, r01, r02, r10, r11, r12, r20, r21, r22 = config.cf:GetComponents()
    local newCF = CFrame.new(newX, y, z, r00, r01, r02, r10, r11, r12, r20, r21, r22)
    return {
        Valid = true,
        Position = newPos,
        CF = newCF,
        Rotation = config.rotation
    }
end

-- Auto join map
if autojoinmap then
    task.spawn(function()
        while autojoinmap do
            if game.PlaceId == 108533757090220 then
                local char = game.Players.LocalPlayer.Character
                if char then
                    local pl = char:FindFirstChild("HumanoidRootPart")
                    local humanoid = char:FindFirstChild("Humanoid")
                    if pl and humanoid then
                        local location = CFrame.new(82, 66, 784)
                        humanoid:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
                        pl.CFrame = location
                    end
                end
            end
            task.wait(1)
        end
    end)
end

-- Auto join christmas
if autojoindojo then
    task.spawn(function()
        while autojoindojo do
            local args = {[1] = "map_space"}
            pcall(function()
                game:GetService("ReplicatedStorage").RemoteFunctions.LobbySetMap_5:InvokeServer(unpack(args))
            end)
            task.wait(1)
        end
    end)
end

-- Auto difficulty
if autodifficulty then
    task.spawn(function()
        while autodifficulty do
            if game.PlaceId ~= 108533757090220 then
                pcall(function()
                    local args = {[1] = "dif_apocalypse"}
                    game:GetService("ReplicatedStorage"):WaitForChild("RemoteFunctions"):WaitForChild("PlaceDifficultyVote"):InvokeServer(unpack(args))
                end)
            end
            task.wait(1)
        end
    end)
end

-- Auto change speed
if autospeedchange then
    task.spawn(function()
        local speedTimer = 0
        while autospeedchange do
            if game.PlaceId ~= 108533757090220 then
                if speedTimer < 5 then
                    pcall(function()
                        local args = {[1] = 3}
                        game:GetService("ReplicatedStorage"):WaitForChild("RemoteFunctions"):WaitForChild("ChangeTickSpeed"):InvokeServer(unpack(args))
                    end)
                    speedTimer = speedTimer + 1
                end
            end
            task.wait(1)
        end
    end)
end

-- Auto skip waves
if autoskipwaves then
    task.spawn(function()
        while autoskipwaves do
            if game.PlaceId ~= 108533757090220 then
                pcall(function()
                    local args = {[1] = "y"}
                    game:GetService("ReplicatedStorage"):WaitForChild("RemoteFunctions"):WaitForChild("SkipWave"):InvokeServer(unpack(args))
                    print("Wave skipped")
                end)
            end
            task.wait(0.2)
        end
    end)
end

-- Auto restart
if autorestart then
    task.spawn(function()
        while autorestart do
            if game.PlaceId ~= 108533757090220 then
                pcall(function()
                    game:GetService("ReplicatedStorage"):WaitForChild("RemoteFunctions"):WaitForChild("RestartGame"):InvokeServer()
                end)
            end
            task.wait(1)
        end
    end)
end

-- Auto win christmas (randomize by 5% of X and Z)
-- Only place when PlaceId is NOT 108533757090220
if autowindojo then
    task.spawn(function()
        while autowindojo do
            local ok, err = pcall(function()
                if game.PlaceId ~= 108533757090220 then
                    local placedCount = getPlacedFlowerCount()
                    flowerCount = placedCount
                    if flowerCount < #flowerConfigs then
                        local config = flowerConfigs[flowerCount + 1]
                        local randomizedPlacement = randomizePosition(config)
                        local args = { "unit_evolution_flower", randomizedPlacement }
                        local success, result = pcall(function()
                            return game:GetService("ReplicatedStorage")
                                :WaitForChild("RemoteFunctions")
                                :WaitForChild("PlaceUnit")
                                :InvokeServer(unpack(args))
                        end)
                        print("Attempting to place Flower ID " .. config.id .. " (Placed: " .. flowerCount .. ", Position: " .. tostring(randomizedPlacement.Position) .. ") Result: " .. tostring(result))
                        -- Wait and verify via UI count
                        task.wait(0.1)
                        local newCount = getPlacedFlowerCount()
                        if newCount > flowerCount then
                            print("? Flower ID " .. config.id .. " placed successfully! New count: " .. newCount)
                        else
                            print("? Flower ID " .. config.id .. " placement failed. Retrying...")
                            task.wait(2)
                        end
                    end
                end
            end)
            if not ok then
                warn("Auto Flower error:", err)
            end
            task.wait(2)
        end
    end)
end

print("GTD script loaded successfully")
print("Anti-AFK enabled")
